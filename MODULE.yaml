# Speech module manifest.
# See modules/README.md for the module standard.
name: speech
version: "1.0.0"
description: Audio capture, STT, TTS, speaker filter, calibration.
order: 10
enabled: true
config_file: config.yaml
docs_path: docs
help_entry: README.md
