# Speech module

Audio capture, STT, TTS, speaker filter, and voice calibration. See the [project wiki — Module Speech](https://github.com/talkie-assistant/talkie-core/wiki/Module-Speech) for full documentation.

- **Main mode:** Press **H** or **h** to show general quick reference.
- **Calibration:** Use the Calibration panel for voice enrollment and settings.

Override config in root `config.yaml` or `config.user.yaml`.
